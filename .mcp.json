{
  "mcpServers": {
    "palace": {
      "command": "cargo",
      "args": [
        "run",
        "--bin", "palace-mcp",
        "--manifest-path", "Cargo.toml",
        "--release"
      ],
      "env": {
        "PALACE_DIMS": "384",
        "RUST_LOG": "palace_mcp=info"
      }
    }
  }
}
